{
    "version": 2,
    "buildCommand": "next build",
    "builds": [
      {
        "src": "server/server.js",
        "use": "@now/node",
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "server/server.js"
      }
    ]
  }